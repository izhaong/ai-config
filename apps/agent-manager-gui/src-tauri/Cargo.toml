[package]
name = "agent-manager-gui-bin"
version = "0.4.1"
edition = "2021"
description = "Tauri 2 binary entry — calls into agent_manager_gui_lib::run"

[[bin]]
name = "agent-manager-gui"
path = "src/main.rs"

[dependencies]
agent-manager-gui = { path = ".." }
tauri = { workspace = true, features = [] }

[build-dependencies]
tauri-build = { workspace = true }
