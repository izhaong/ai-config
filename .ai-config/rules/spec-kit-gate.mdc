---
description: Spec Kit 贴合 Cursor — spec/plan 落盘，Todos 与 Plan 一体；用户傻瓜式下发任务
alwaysApply: true
---

# Spec Kit 门禁（贴合 Cursor）

**前提**：Cursor Agent 行为（Plan、Todo、多步执行）不可改造；**Spec Kit 适配 Cursor**，不造平行流程。

## Cursor ↔ Spec Kit 映射

| Cursor（会话内） | Spec Kit（`specs/`，落盘）                      |
| ---------------- | ----------------------------------------------- |
| 用户一句话       | `spec.md` — 要什么、怎么验收                    |
| Plan             | `plan.md` 正文 — 技术方案                       |
| Todo 列表        | `plan.md` → **`## Todos`**（`- [ ]` / `- [x]`） |
| 按 Todo 实现     | 读 `plan.md ## Todos` 顺序写码                  |
| 会话结束         | `spec.md` + `plan.md` 留在 git                  |

**不维护 `tasks.md`**。执行清单只在 `plan.md ## Todos`。

## 用户侧零负担

- 用户只说任务；**禁止**要求 `/speckit.*`、`export SPECIFY_FEATURE`、固定话术。
- Agent 落盘 spec/plan，可选将 `## Todos` 镜像到 Cursor Todo（若使用 TodoWrite）；完成时回写 `plan.md` 勾选。

## Agent 收到任务 — 自动分流

### 1. 例外（直接实现）

- 单文件 typo、一行明显 bugfix、纯 fmt/lint
- 用户明确 **spike**

### 2. 非平凡（spec → plan 含 Todos → 实现）

满足任一即非平凡：新功能、改架构、跨 crate、新 CLI/GUI、批量业务改动。

**按序完成**（同会话，不等用户）：

1. **分支** — [gitflow-spec-kit.mdc](./gitflow-spec-kit.mdc)
2. **FEATURE_DIR** — 下文「自动解析」
3. **规格**（`.specify/templates/`、`constitution.md`）：
   - 无 `spec.md` → 用户场景 + 可测需求
   - 无 `plan.md` 或无 `## Todos` → **详细**技术方案 + Todos（`- [ ] T00n …（验证：…）`）；末项 verify
   - **plan 详细度**（模板见 `plan-template.md`、命令见 `speckit.plan`）：须含 Summary、根因（文件/函数级）、影响面与 FR 追溯、方案设计、边界表、测试策略、回滚；禁止仅模块表 + 笼统 Todo
4. **实现** — 按 Todos 顺序；每项完成 → `plan.md` 勾 `- [x]`
5. **验证** — [ai-config-verify](../skills/ai-config-verify/SKILL.md)

有 `plan.md ## Todos` 前**禁止**写业务代码。

## FEATURE_DIR 自动解析

用户无需 `export SPECIFY_FEATURE`。Agent 自用 `export SPECIFY_FEATURE=<目录名>`：

| 优先级 | 条件                   | FEATURE_DIR                                                 |
| ------ | ---------------------- | ----------------------------------------------------------- |
| 1      | 已设 `SPECIFY_FEATURE` | `specs/$SPECIFY_FEATURE`                                    |
| 2      | 分支 `NNN-*`           | `specs/` 下唯一 `NNN-*` 目录                                |
| 3      | `feat/*` / `fix/*`     | 按 slug / 关键词 / 最近修改匹配 `specs/`                    |
| 4      | 无匹配                 | `create-new-feature.sh --json --short-name <slug> '<摘要>'` |

逻辑见 `.specify/scripts/bash/common.sh`。

## 已有规格时

- 对齐 `spec.md` + `plan.md`（含 `## Todos`）
- 用户任务与 spec 冲突 → 先改 spec/plan，再写码

## 与 Claude Code / 全局规则

- Claude 读 `AGENTS.md` + 本规则；落盘结构相同，语义对齐 Cursor Plan/Todo。
- 本仓 `.ai-config/rules/spec-kit-gate.mdc` + `gitflow-spec-kit.mdc` 为 **ai-config 权威**；与 `~/.cursor/rules/spec-kit-sdd-gate.mdc` 一致，冲突时以本仓为准。
