---
description: agents-manager 核心工作流：crate 边界、验证、Git、文档同步
alwaysApply: true
---

# agents-manager 核心规则

## 仓库与 cwd

| 路径                                         | 职责                                                                   |
| -------------------------------------------- | ---------------------------------------------------------------------- |
| `crates/agents-manager-core`                      | **唯一业务逻辑**（model / source / platform / sync / link / template） |
| `crates/agents-manager-cli`                       | clap 子命令，薄封装调用 core                                           |
| `crates/agents-manager-daemon`                    | 长驻 + 文件监听                                                        |
| `crates/agents-manager-store` / `bus` / `watcher` | 存储与事件                                                             |
| `apps/agents-manager-gui`                         | Tauri 2 + React；**仅**通过 `invoke` 调 Rust 命令                      |

改代码时 **cwd 为正在修改的 crate 或 app 根**；workspace 级命令在仓库根执行。

## 依赖选型

- **GUI**：ahooks、es-toolkit、shadcn/ui、lucide-react — 见 `gui-agents-manager.mdc`
- **Rust**：workspace 成熟 crate（serde、thiserror、tokio、itertools 等）— 见 `rust-agents-manager.mdc`
- 优先高星、维护活跃的库；不重复造轮子

## 架构约束

- GUI / CLI **不得**复制 core 已有逻辑；新能力先进 `agents-manager-core`，再暴露 CLI 子命令与 Tauri command。
- 平台路径与 install 语义见 `crates/agents-manager-core/src/platform.rs`。
- 用户资产在 `~/.agents-manager/`；本仓只维护工具与模板。
- Hook 物理布局（单文件 / 目录整包、可同步单元）见 [hooks-asset-layout.mdc](./hooks-asset-layout.mdc)。

## 开工前（Agent 自治，用户无感）

用户用自然语言下发任务即可。Agent **自行**：

1. `git branch --show-current`；在 `develop`/`main` 且任务非平凡 → 自动切 feature 分支（见 [gitflow-spec-kit.mdc](./gitflow-spec-kit.mdc)）
2. 解析 `FEATURE_DIR`、补齐 `spec.md` 与含 `## Todos` 的 `plan.md` 后再写业务代码（见 [spec-kit-gate.mdc](./spec-kit-gate.mdc)）
3. 边界类改动前读 `docs/product/ARCHITECTURE.md` 相关章节

**禁止**要求用户跑 `/speckit.*`、`export SPECIFY_FEATURE` 或确认切分支。

## 验证（完成前必做）

- Core/CLI：`cargo test -p agents-manager-core -p agents-manager-cli`（**新功能须同步新增/更新对应用例**）
- GUI：`cd apps/agents-manager-gui && npm run test && npm run build`
- 同步相关：`agents-manager doctor`、`agents-manager status`

## 文档

- CLI/行为变更 → `CHANGELOG.md`
- 路由/Agent 约定变更 → `AGENTS.md`
- 产品语义变更 → `docs/product/`

## Git

- 从 `develop` 拉 feature 分支：`NNN-<name>`（`create-new-feature.sh` 首选）或 `feat/*` / `fix/*`
- 中文 Conventional Commits：`<type>(<scope>): <描述>`
- 未要求时不主动 commit/push

## 敏感信息

禁止提交真实 token、密钥、`secrets.env`；示例用 `<REDACTED>`。
